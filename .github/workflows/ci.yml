name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  FLUTTER_VERSION: 3.47.1

jobs:
  quality:
    name: Analyze and test
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out source
        uses: actions/checkout@v6
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true
      - name: Verify workspace
        run: ./tool/verify.sh

  android:
    name: Android debug APK
    needs: quality
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - name: Check out source
        uses: actions/checkout@v6
      - name: Set up Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 17
          cache: gradle
          cache-dependency-path: apps/example_client/android/**/*.gradle*
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true
      - name: Set up pinned Android SDK
        uses: android-actions/setup-android@v4
        with:
          packages: >-
            platform-tools
            platforms;android-36
            build-tools;36.0.0
            ndk;28.2.13676358
      - name: Build APK
        working-directory: apps/example_client
        run: flutter build apk --debug
      - name: Upload APK
        uses: actions/upload-artifact@v7
        with:
          name: dakit-example-android-debug
          path: apps/example_client/build/app/outputs/flutter-apk/app-debug.apk
          if-no-files-found: error

  macos:
    name: macOS debug app
    needs: quality
    runs-on: macos-15
    timeout-minutes: 35
    steps:
      - name: Check out source
        uses: actions/checkout@v6
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true
      - name: Build macOS app
        working-directory: apps/example_client
        run: flutter build macos --debug
      - name: Upload macOS app
        uses: actions/upload-artifact@v7
        with:
          name: dakit-example-macos-debug
          path: apps/example_client/build/macos/Build/Products/Debug/example_client.app
          if-no-files-found: error

  windows:
    name: Windows app and MSIX
    needs: quality
    runs-on: windows-2025
    timeout-minutes: 40
    steps:
      - name: Check out source
        uses: actions/checkout@v6
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true
      - name: Build Windows app
        working-directory: apps/example_client
        run: flutter build windows --release
      - name: Create development MSIX
        working-directory: apps/example_client
        run: dart run msix:create --build-windows false --install-certificate false
      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v7
        with:
          name: dakit-example-windows
          path: |
            apps/example_client/build/windows/x64/runner/Release/**
            apps/example_client/build/windows/**/*.msix
          if-no-files-found: error
